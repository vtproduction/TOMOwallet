<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tomoapp.tomowallet">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CAMERA"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".base.MainApplication">
        <meta-data android:name="firebase_crashlytics_collection_enabled" android:value="false" />

        <activity android:name=".ui.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.createWallet.CreateWalletActivity" />
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.tmcInfo.TMCInfoActivity" />
        <activity android:name=".ui.ethInfo.ETHInfoActivity" />
        <activity android:name=".ui.receive_tmc.ReceiveTMCActivity" />
        <activity android:name=".ui.send_tmc.SendTMCActivity" />
        <activity android:name=".ui.send_tmc.QRCodeScanActivity"/>
        <activity android:name=".ui.createPincode.CreatePinCodeActivity" />
        <activity android:name=".ui.checkPassword.CheckPasswordActivity" />
        <activity android:name=".ui.importWallet.ImportWalletActivity" />
        <activity android:name=".ui.createWallet.CreateWalletInstructionActivity" />
        <activity android:name=".ui.createPassword.CreatePasswordActivity" />
        <activity android:name=".ui.backupWallet.BackupWalletActivity" />
        <activity android:name=".ui.importWallet.ImportWalletSuccessActivity" />
    </application>

</manifest>